import styled from 'styled-components'

const ContainerApp = styled.div`
    background: #e0e0e0;
    height: 100vh;
    position: absolute;
    width: 100%;
`


export const AppStyled = {
    ContainerApp
}